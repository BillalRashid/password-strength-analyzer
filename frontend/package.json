{
  "name": "password-strength-analyzer-frontend",
  "version": "1.0.0",
  "private": true,
  "homepage": ".", 
  "dependencies": {
    "@react-oauth/google": "^0.7.0",
    "axios": "^1.4.0",
    "bootstrap": "^5.3.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.6.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",             // Start local dev server
    "build": "react-scripts build",             // Build for production
    "test": "react-scripts test",               // Run tests
    "eject": "react-scripts eject",             // Eject config (use carefully)
    "predeploy": "npm run build",               // Used in deployment pipelines
    "deploy": "gh-pages -d build"               // Optional: for GitHub Pages (if needed)
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
